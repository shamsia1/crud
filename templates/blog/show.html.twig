{% extends 'base.html.twig' %}

{% block body%}
<article>
		<h2>{{ article.title }}</h2>
		<div class="metadata">
			Ecrit le {{ article.createdAt | date('d/m/y')}} a {{ article.createdAt | date('d/m/y')}} dand le categorie politique
		</div>
		<div class="content">
			<img src="{{ article.image }}">

			{{ article.content | raw }}

		</div>
	</article>
{% endblock %}